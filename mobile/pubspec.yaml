name: liberty_messenger
description: Liberty Messenger — Sovereign P2P messenger with Hidden Vault
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_secure_storage: ^9.0.0
  local_auth: ^2.2.0
  provider: ^6.1.1
  uuid: ^4.3.3
  intl: ^0.19.0
  path_provider: ^2.1.2
  qr_flutter: ^4.1.0
  file_picker: ^8.0.0
  image_picker: ^1.0.7
  flutter_local_notifications: ^17.0.0
  path: ^1.9.0
  record: ^6.1.0
  audioplayers: ^6.0.0
  permission_handler: ^11.3.0
  http: ^1.2.0
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

dependency_overrides:
  record_linux: ^1.1.1

flutter:
  uses-material-design: true
