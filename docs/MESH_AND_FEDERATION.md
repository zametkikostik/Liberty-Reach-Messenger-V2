# Mesh and Federation

## Mesh?
Yes at libp2p layer: mDNS (LAN), Gossipsub, Kademlia DHT, bootstrap list.

## Federation
Anyone can run bootstrap/wake. App: Federation & Mesh → Add peer. Updates by name do not drop other peers.
Modes: Local mesh / Federated / Private.
