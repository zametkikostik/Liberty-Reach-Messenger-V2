# Cloudflare Wake Relay (no Firebase)

API: POST /v1/register, /v1/wake, /v1/poll; GET /privacy, /health
No plaintext allowed on wake.
Deploy: ./scripts/deploy_worker.sh
App: Security → Wake relay URL
